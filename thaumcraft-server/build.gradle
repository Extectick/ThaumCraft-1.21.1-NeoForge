plugins {
    id 'base'
}

tasks.named('assemble') {
    dependsOn rootProject.tasks.named('serverJar')
}

tasks.named('build') {
    dependsOn rootProject.tasks.named('serverJar')
    dependsOn rootProject.tasks.named('checkVariantJars')
}
